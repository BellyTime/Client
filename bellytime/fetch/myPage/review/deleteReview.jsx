import axios from "axios";
export const deleteReview = async (reservationId) => {
  try {
    // const list = await axios.post("../../static/dummyData/myReview.json",{reservationId});
    // console.log(list.data);
    // return list.data;
    console.log(reservationId);
  } catch (e) {
    console.log(e);
  }
};
